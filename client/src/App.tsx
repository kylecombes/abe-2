import React from 'react';
import { Calendar } from './Calendar';

interface AppProps {}

function App({}: AppProps) {
  return (
    <div className="App">
      <Calendar/>
    </div>
  );
}

export default App;
